import React from 'react';

import DisplayNote from './DisplayNote';

const Main = props => {
    let MainContent = 

    return (
        <main>
            {MainContent}
        </main>
    );
}

export default Main; 